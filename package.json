{
  "name": "pact",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:provider": "cross-env PORT=3001 node provider/provider-service.js",
    "test:web:consumer": "jest consumer-web/consumer-web.spec.js",
    "publish:pact": "pact-broker publish ./pacts --consumer-app-version=$GIT_COMMIT_SHA --tag=main --broker-base-url=$PACT_BROKER_URL --broker-token=$PACT_API_TOKEN",
    "test:provider": "jest provider/provider-contract.spec.js --testTimeout=5000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4.18.2",
    "joi": "17.10.2"
  },
  "devDependencies": {
    "@pact-foundation/pact": "12.1.0",
    "axios": "1.5.1",
    "cross-env": "^7.0.3",
    "jest": "29.7.0"
  }
}
